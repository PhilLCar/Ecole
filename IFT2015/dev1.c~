char* compress(char* text) {
  char temp[50];
  char c;
  int word = 0;
  while ((c = getchar()) != EOF) {
    if (c == ' ' || c == '\t' || c == '\n') {
      if (word) {
	addWord(temp)
      } else continue;
    }
  }
}

/*
  Le but est de remplacer tous les mots par un index d'un des deux tableaux, la forme est la suivante

  FLAG FORM:
  000000
  ^^^^^^
  |||||
  ||||ALL CAPS
  |||CAPS
  ||S TERMINATED
  |
  COMPRESSION SWITCH

  LONG WORD:  (3 bytes)   // When length > 2
  TYPE  FLAGS     POSITION
  00    FFFFFF    IIII IIII IIII IIII

  SHORT WORD: (2 byte)    // When length == 2
  TYPE  FLAGS     POSITION
  01    FFFFFF    IIII IIII

  LETTER:     (1 byte)   // When length < 2
  TYPE  CAPS  LETTER
  10    C     LLLLL

  SYMBOL:
  TYPE  CAPS  SYMBOL
  11    C     SSSSS

  SEGMENT:    (~ bytes)  // When impossible to compress
  001X XXXX XXXX XXXX ... 001X XXXX XXXX XXXX
 */
